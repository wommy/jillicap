# jillian salloum - capstone

### install pnpm

- `npm i -g pnpm`

### install node packages

- `pnpm i`

### run front and back recursively

- `pnpm -r start`

or either `cd vite` or `cd sanity` && `pnpm start`


